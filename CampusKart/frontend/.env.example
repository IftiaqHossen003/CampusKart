VITE_API_URL=/api/v1
VITE_WS_URL=ws://localhost/ws
# Optional Vite dev proxy targets.
# VITE_API_PROXY_TARGET=http://nginx
# VITE_WS_PROXY_TARGET=ws://nginx
# Optional when VITE_WS_URL is not set. Defaults to 8001 in the app.
# VITE_WS_PORT=8001